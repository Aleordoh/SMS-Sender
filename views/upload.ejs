<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            padding: 40px;
        }

        h1 {
            color: #333;
            margin-bottom: 10px;
            font-size: 28px;
        }

        h2 {
            color: #555;
            margin-bottom: 20px;
            font-size: 20px;
            font-weight: normal;
        }

        .nav {
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #f0f0f0;
        }

        .nav a {
            display: inline-block;
            padding: 10px 20px;
            margin-right: 10px;
            background: #667eea;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s;
        }

        .nav a:hover {
            background: #764ba2;
        }

        .alert {
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 5px;
            white-space: pre-line;
        }

        .alert-error {
            background: #fee;
            color: #c33;
            border: 1px solid #fcc;
        }

        .alert-success {
            background: #efe;
            color: #3c3;
            border: 1px solid #cfc;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: 500;
        }

        input[type="text"],
        input[type="number"],
        input[type="password"],
        input[type="file"] {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 5px;
            font-size: 14px;
            transition: border-color 0.3s;
        }

        input[type="text"]:focus,
        input[type="number"]:focus,
        input[type="password"]:focus,
        input[type="file"]:focus {
            outline: none;
            border-color: #667eea;
        }

        button {
            width: 100%;
            padding: 15px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #764ba2;
        }

        .file-info {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .file-info h3 {
            color: #667eea;
            margin-bottom: 10px;
        }

        .file-info ul {
            margin-left: 20px;
        }

        .file-info li {
            margin-bottom: 5px;
            color: #666;
        }

        .gateway-config {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
            margin-bottom: 20px;
        }

        .gateway-config h3 {
            color: #667eea;
            margin-bottom: 15px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        footer {
            margin-top: 40px;
            padding-top: 20px;
            border-top: 2px solid #f0f0f0;
            text-align: center;
            color: #999;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        
        <div class="nav">
            <a href="/sms">📤 Enviar SMS</a>
            <a href="/sms/config">⚙️ Configuración</a>
        </div>

        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>

        <% if (success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>

        <div class="file-info">
            <h3>📋 Formato del Archivo</h3>
            <ul>
                <li>Formatos aceptados: XLSX, XLS, CSV</li>
                <li>Columnas requeridas: <strong>phone</strong> (o telephone/number) y <strong>message</strong> (o text/sms)</li>
                <li>Tamaño máximo: 5MB</li>
                <li>Los mensajes no deben exceder 160 caracteres</li>
            </ul>
        </div>

        <form action="/sms/upload" method="POST" enctype="multipart/form-data">
            <div class="gateway-config">
                <h3>⚙️ Configuración del Gateway</h3>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="gateway_host">Host/IP:</label>
                        <input type="text" id="gateway_host" name="gateway_host" placeholder="192.168.1.100" value="<%= process.env.GATEWAY_HOST || '' %>">
                    </div>
                    
                    <div class="form-group">
                        <label for="gateway_port">Puerto:</label>
                        <input type="number" id="gateway_port" name="gateway_port" placeholder="80" value="<%= process.env.GATEWAY_PORT || '80' %>">
                    </div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="gateway_username">Usuario:</label>
                        <input type="text" id="gateway_username" name="gateway_username" placeholder="admin" value="<%= process.env.GATEWAY_USERNAME || 'admin' %>">
                    </div>
                    
                    <div class="form-group">
                        <label for="gateway_password">Contraseña:</label>
                        <input type="password" id="gateway_password" name="gateway_password" placeholder="admin" value="<%= process.env.GATEWAY_PASSWORD || '' %>">
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label for="file">📁 Seleccionar Archivo (XLSX o CSV):</label>
                <input type="file" id="file" name="file" accept=".xlsx,.xls,.csv" required>
            </div>

            <button type="submit">🚀 Enviar SMS Masivos</button>
        </form>

        <footer>
            <p>SMS Sender - Synway SMG4008-8WA Gateway</p>
        </footer>
    </div>
</body>
</html>
