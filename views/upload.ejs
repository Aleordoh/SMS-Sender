<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/main.css">

</head>

<body>
  <div class="container">
    <h1><%= title %></h1>

    <div class="nav">
      <a href="/sms">📤 Enviar SMS</a>
      <a href="/sms/inbox">📨 Mensajes Recibidos</a>
      <a href="/sms/config">⚙️ Configuración</a>      
    </div>

    <% if (error) { %>
    <div class="alert alert-error">
      <%= error %>
    </div>
    <% } %>

    <% if (success) { %>
    <div class="alert alert-success">
      <%= success %>
    </div>
    <% } %>

    <div class="file-info">
      <h3>📋 Formato del Archivo</h3>
      <ul>
        <li>Formatos aceptados: XLSX, XLS, CSV</li>
        <li>Columnas requeridas: <strong>phone</strong> (o telephone/number) y <strong>message</strong> (o text/sms)</li>
        <li>Tamaño máximo: 5MB</li>
        <li>Los mensajes no deben exceder 160 caracteres</li>
      </ul>
      <p style="margin-top: 10px; padding: 10px; background: #f0f4ff; border-radius: 5px; color: #333;">
        <strong>⚙️ Configuración actual:</strong> Los mensajes se enviarán secuencialmente usando <strong><%= portCount %> puerto(s)</strong> GSM. 
        <a href="/sms/config" style="color: #667eea; text-decoration: underline;">Cambiar configuración</a>
      </p>
    </div>

    <form action="/sms/upload" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label for="file">📁 Seleccionar Archivo (XLSX o CSV):</label>
        <input type="file" id="file" name="file" accept=".xlsx,.xls,.csv" required>
      </div>

      <button type="submit">🚀 Enviar SMS Masivos</button>
    </form>

    <footer>
      <p>SMS Sender - Synway SMG4008-8WA Gateway</p>
    </footer>
  </div>
</body>

</html>